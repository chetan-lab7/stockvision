# Server Configuration
PORT=5000

# MongoDB Connection
# For local MongoDB
MONGODB_URI=mongodb://localhost:27017/stockvision
# For MongoDB Atlas
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/stockvision?retryWrites=true&w=majority

# JWT Secret (used for authentication)
# Replace this with a strong random string for production
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Optional: API Keys for external data sources
# STOCK_API_KEY=your-stock-api-key 